langgraph==1.2.8
langgraph-checkpoint-sqlite==3.1.0
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
pgvector==0.5.0
langchain-core==1.4.8
langchain-groq==1.1.3
pydantic==2.13.4
streamlit==1.59.0
gspread==6.2.1
google-auth==2.55.1
google-auth-oauthlib==1.4.0
google-api-python-client==2.198.0
google-genai==2.10.0
tavily-python==0.7.26
requests==2.34.2
pymupdf==1.28.0
python-docx==1.2.0
openpyxl==3.1.5
segno==1.6.6
python-dotenv==1.2.2
stripe==15.3.1
fastapi==0.139.2
uvicorn[standard]==0.50.2
prometheus-client==0.25.0

# Planchers de sécurité sur des dépendances TRANSITIVES (§15.3.8, scan du 2026-07-26).
# Ni gitpython ni pyasn1 ne sont importés par ce projet : ils arrivent via streamlit et google-auth.
# `pip-audit` y a trouvé 8 et 3 vulnérabilités connues sur les versions que pip avait résolues
# (gitpython 3.1.50, pyasn1 0.6.3). Un `pip install -r requirements.txt` sur une machine neuve
# aurait pu les réinstaller, puisque rien ici ne disait le contraire : d'où ces planchers explicites
# (`>=` et non `==`, pour ne pas figer une dépendance dont ce projet ne pilote pas le cycle de vie).
gitpython>=3.1.55
pyasn1>=0.6.4

# Dev / tests
pytest==9.1.1
pip-audit==2.10.1
